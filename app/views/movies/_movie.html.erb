<a href="<%= movie_path(movie) %>" class="group col-span-1 flex flex-col divide-y divide-gray-200 rounded-lg bg-white text-center shadow hover:bg-red-50">
  <div class="flex flex-1 flex-col p-4">
    <img class="mx-auto h-32 w-22 flex-shrink-0 rounded-md" src="<%= movie.full_poster_path %>">
    <h3 class="mt-6 text-sm font-medium text-gray-900"><%= movie.title %></h3>
    <dl class="mt-1 flex flex-grow flex-col justify-between">
      <dt class="sr-only">Title</dt>
    </dl>
  </div>
</a>
